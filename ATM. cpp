#include<bits/stdc++.h>
using namespace std; 
int main(){
	unsigned int a; 
	double b,ans=0.00; 
	cin>>a>>b; 
	if(a%5==0){
		if((a+0.5)<=b){
			ans=b-a-0.5;
			printf("%.2f",ans);
		}
		else{
		   printf("%.2lf",b);
		}
	}
	else{
		printf("%.2lf",b) ;
	}
	return 0;
}